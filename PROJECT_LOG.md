<!--
Это копия рабочего файла памяти проекта (ведётся в Claude Code вне репозитория,
на уровень выше — в папке проекта на диске у Ирины). Синхронизируется вручную при пуше,
чтобы история решений была доступна и без доступа к этой папке.
-->

# CLAUDE.md — память проекта

Этот файл — единственное место, где хранится контекст проекта между сессиями.
Дополняется по ходу работы без напоминаний от пользователя.

## О проекте

Сайт-визитка + портфолио для художника-витражиста.
Одностраничный лендинг (главная) + отдельные страницы «Обо мне», «Контакты», «Портфолио».

- Название сайта: **ART.OHREY** — арт-студия современного витража
- Автор/художник: **Ирина Павлова**
- Специализация/ниша: современный витраж как самостоятельный предмет интерьера (интерьерные витражные панно/картины для квартир и домов).
  **Важно:** НЕ витражи в храмах, церковных и ритуальных сооружениях, зданиях городской инфраструктуры; НЕ оформление окон/дверей — только самостоятельные интерьерные арт-объекты.
- Услуги: изготовление современных витражей под заказ + продажа готовых витражей
- Цветовая палитра: холодная (глубокий синий/фиолетовый, «готический»-эстетика, но в современном прочтении — не церковная тематика)
- Тон текстов: дружелюбный и динамичный (неформальный, «на ты» или лёгкое «вы» — уточним при написании текстов)
- Секции главной страницы (актуально): **Обо мне → Услуги → Портфолио → Контакты**. Hero, отзывы, цены, FAQ — пока не делаем, оставляем задел в структуре компонентов на будущее, но на главную не выводим.

## Стек (зафиксирован, не меняется)

- Next.js (App Router) + React + TypeScript, последние стабильные версии
- Серверные компоненты по умолчанию, клиентские — только где обязательно (интерактив: карусель, переключатель темы, формы)
- Tailwind CSS
- shadcn/ui — все интерфейсные примитивы (`npx shadcn@latest add ...`)
- next-themes — светлая/тёмная тема
- next/font — шрифты
- next/image — все изображения, портфолио через SSR
- Контент-данные (отзывы, ссылки, услуги, цены и т.п.) — JSON-файлы в `content/`. Без базы данных.
- Статьи (если появятся) — `.mdx`
- Никакой CMS, никакой авторизации, никаких личных кабинетов, админку не делаем

Alternatives не рассматриваем: Vue, Svelte, Astro, WordPress — решение окончательное.

## Дизайн

- Ничего не изобретаем с нуля. Интерфейсные элементы — только shadcn/ui.
- Готовые секции (hero, features, pricing, testimonials, CTA, FAQ, footer) — из открытых коллекций блоков (Tailark, Magic UI, shadcn.io). Если готового блока нет — собираем из shadcn-компонентов, не выходя за рамки их стиля.
- Светлая/тёмная тема через next-themes.
- Анимации — лёгкие, без перегруза.
- Портфолио — карусель фотографий витражных картин, у каждой фото — своё описание.

## SEO (делаем сразу, не откладываем)

- `app/sitemap.ts`, `app/robots.ts`
- `generateMetadata` на каждой странице: уникальные title, description, OG-картинка
- JSON-LD: `BlogPosting` (статьи), `Person`/`Organization` (главная), `Review` (отзывы), `BreadcrumbList` (навигация)
- Canonical URL на всех страницах
- Все картинки через `next/image` (AVIF/WebP, ленивая загрузка)
- После деплоя — пинг IndexNow со списком обновлённых URL (Яндекс, Bing)

## Инфраструктура и деплой

- Хостинг: VPS на reg.ru, Ubuntu 26.04 LTS — подтверждено по SSH, root-доступ есть. (Первый купленный тариф оказался shared-хостингом с ISPmanager без root — см. в логе решений; сейчас используется второй, настоящий VPS.)
- ⚠️ Сервер маленький: 1 CPU, ~1 ГБ RAM, поэтому Docker-образ **не собирается на сервере** — собирается в GitHub Actions и публикуется в GitHub Container Registry (`ghcr.io/morkelebthennei-hub/site_pavlova`), сервер только скачивает готовый образ. `deploy/vps-setup.sh` заводит 2 ГБ swap-файла для страховки.
- Docker: multi-stage build (`deploy/Dockerfile`), `output: standalone` в `next.config.ts`. `deploy/docker-compose.yml` использует `image:` (не `build:`).
- Nginx как reverse proxy: HTTPS + gzip (`deploy/nginx.conf`, домен art-ohrey.art)
- Let's Encrypt через certbot, автопродление
- GitHub Actions (`.github/workflows/deploy.yml`): `build` (lint+build) → `image` (сборка и push в ghcr.io при пуше в main) → `deploy` (SSH: git pull + docker compose pull/up + healthcheck; пропускается без секретов SSH_HOST/SSH_USER/SSH_PRIVATE_KEY)
- Скрипт первичной настройки VPS — `deploy/vps-setup.sh`: swap, Docker, nginx, certbot, клонирование репозитория

### Доступы

Реальные значения (IP, логин, путь к SSH-ключу) хранятся только в приватном рабочем файле вне этого репозитория — репозиторий публичный, секреты и данные для входа сюда не публикуем.

- Домен: art-ohrey.art (куплен; DNS пока переключается на новый сервер)
- GHCR-образ: ghcr.io/morkelebthennei-hub/site_pavlova — публичный
- GitHub-репозиторий: https://github.com/morkelebthennei-hub/site_pavlova

### Статус деплоя (2026-09-12)

✅ Сайт задеплоен и работает на сервере — контейнер запущен, nginx проксирует, автодеплой через GitHub Actions настроен и проверен (пуш в `main` → сборка образа → SSH → `docker compose pull/up` → healthcheck).

Осталось: переключить DNS домена на новый сервер (делает пользователь в панели reg.ru), затем выпустить HTTPS через certbot.

## Структура папок (план, уточняется по ходу работы)

```
site-project/
├── CLAUDE.md
├── app/
│   ├── layout.tsx
│   ├── page.tsx                 # главная
│   ├── about/page.tsx           # обо мне
│   ├── contacts/page.tsx        # контакты
│   ├── portfolio/page.tsx       # портфолио
│   ├── sitemap.ts
│   ├── robots.ts
│   └── (blog)/... (если появятся mdx-статьи)
├── components/
│   ├── ui/                      # shadcn/ui компоненты
│   ├── sections/                # about-hero (обо мне + CTA), services, portfolio-preview, contacts, footer
│   │                             # (testimonials/pricing/faq — задел на будущее, сейчас не используются)
│   └── portfolio/                # карусель и карточка работы
├── content/
│   ├── testimonials.json
│   ├── services.json
│   ├── pricing.json
│   └── portfolio.json
├── public/
│   └── images/
├── .github/workflows/deploy.yml
├── deploy/
│   ├── Dockerfile
│   ├── docker-compose.yml
│   ├── nginx.conf
│   └── vps-setup.sh
└── next.config.ts
```

## Правила работы

1. Перед крупной задачей — план (какие файлы создаются/меняются, зависимости). Старт только после «делай».
2. Маленькие итерации, после каждой — видимый результат.
3. После правок — `npm run dev`, проверка что сайт открывается без ошибок. Проблемы чиню сам.
4. Перед коммитом — самопроверка: без мусора и закомментированного кода.
5. Коммиты короткие и по одной задаче.
6. Термины объясняю простым языком, жаргон сразу расшифровываю.
7. Всё, что касается движка/стека, бэкенда, дизайна, доступов и деплоя — сразу фиксирую здесь, в «Лог решений» и в соответствующих разделах выше, без напоминаний. При каждом пуше в GitHub синхронизирую этот файл с `site-project/PROJECT_LOG.md` (копия для репозитория).

## Лог решений

- 2026-09-12: зафиксирован стек, подход к дизайну и инфраструктуре. Создан CLAUDE.md.
- 2026-09-12: название — ART.OHREY, автор — Ирина Павлова. Ниша строго ограничена: современный интерьерный витраж-арт-объект, НЕ церковная/ритуальная тематика, НЕ оформление окон/дверей. Палитра — холодная. Тон — дружелюбный/динамичный. Главная = только Обо мне (со встроенным hero+CTA) → Услуги → Портфолио → Контакты. Отзывы/цены/FAQ/отдельный hero — в задел, не в MVP.
- 2026-09-12: логотип — текстовая заглушка (шрифтовой логотип ART.OHREY), дизайн знака делаем в самом конце проекта, отдельным этапом. Фото портфолио — заглушки, генерируем сами: тема «оригинальные супергеройские силуэты» (не Marvel/DC, без чужой IP — только собственные придуманные персонажи/силуэты в витражной стилистике). Реальные фото работ Ирина пришлёт позже, заменим в `content/portfolio.json`.
- 2026-09-12: установлен Node.js LTS (через winget) — на машине его не было. Инициализирован `site-project/`: Next.js 16.3.5 (App Router, Turbopack), React 19.2.8, TypeScript, Tailwind v4, shadcn/ui (style `base-nova`, на базе `@base-ui/react`, иконки lucide), next-themes. Собран рабочий скелет: `app/layout.tsx` с ThemeProvider и шрифтом Geist (кириллица подключена), `components/sections/site-header.tsx` и `site-footer.tsx`, переключатель темы (`components/theme-toggle.tsx`). Главная (`app/page.tsx`) — заглушки секций Обо мне/Услуги/Портфолио/Контакты. Созданы отдельные страницы `app/about`, `app/portfolio`, `app/contacts` с уникальными `metadata` (title/description). Структура `content/*.json` создана пустыми массивами-заглушками. Проверено `npm run dev` — открывается без ошибок, тема и навигация работают.
- 2026-09-12: в навигацию header добавлена якорная ссылка «Услуги» → `/#services` (отдельной страницы для услуг не будет, только секция на главной — так и осталось по плану).
- 2026-09-12: применена холодная «готическая» цветовая палитра (глубокий синий/фиолетовый, oklch) в `app/globals.css` для светлой и тёмной темы вместо стандартной shadcn neutral. Собрана секция «Обо мне» (hero + CTA) — `components/sections/about-hero.tsx`: заголовок, подзаголовок, две CTA-кнопки (на /portfolio и /contacts) и декоративный градиентный блок-плейсхолдер вместо фото. Важный технический момент: кнопки shadcn в этом проекте построены на `@base-ui/react` (не Radix) — при рендере кнопки как ссылки (`render={<Link href="..." />}`) обязательно добавлять `nativeButton={false}`, иначе Base UI ругается в консоли, что ожидает нативный `<button>`.
- 2026-09-12: собрана секция «Услуги» на главной — `components/sections/services.tsx`, добавлен shadcn-компонент `card`. Контент вынесен в `content/services.json` (два пункта: «Витраж на заказ» и «Готовые витражи» — прямо из описания услуг в этом файле). Проверено в обеих темах, ошибок нет.
- 2026-09-12: собрана секция «Портфолио» (превью с каруселью) на главной. Сгенерированы 5 SVG-заглушек витражей в `public/images/portfolio/` (сюжет: собственные придуманные силуэты в духе супергероев — «Страж рассвета», «Хранительница глубин», «Ночной клинок», «Искра», «Страж щита»; техника — радиальная «розетка» из витражных граней + силуэт-фигура, только геометрия, никакого заимствованного IP). Важно: в одном из вариантов случайно получилась эмблема-крест на щите — заменил на ромб, т.к. крест читается как религиозная символика, а ниша строго светская (см. запрет на храмовую тематику). Контент — `content/portfolio.json` (id/title/description/image). Компоненты: `components/portfolio/portfolio-card.tsx`, `portfolio-carousel.tsx` (клиентский, на shadcn `carousel`/Embla), `components/sections/portfolio-preview.tsx`. В `next.config.ts` временно включён `images.dangerouslyAllowSVG` — нужен, чтобы next/image отдавал локальные SVG-заглушки; убрать, когда Ирина пришлёт реальные фото (JPG/WebP) взамен.
- 2026-09-12: пронумеровал заглушки портфолио для удобных запросов на замену. Соответствие (позиция в карусели = порядок в `content/portfolio.json` = номер заглушки), номер виден прямо на карточке (бейдж в углу превью, поле `placeholder` в JSON):
  - заглушка1 = «Страж рассвета» = `public/images/portfolio/01-dawn-guardian.svg`
  - заглушка2 = «Хранительница глубин» = `public/images/portfolio/02-deep-guardian.svg`
  - заглушка3 = «Ночной клинок» = `public/images/portfolio/03-night-blade.svg`
  - заглушка4 = «Искра» = `public/images/portfolio/04-spark.svg`
  - заглушка5 = «Страж щита» = `public/images/portfolio/05-shield-warden.svg`
  Чтобы заменить: просто попросить «замени заглушку2 на фото по пути ...» — обновится `image` соответствующей записи в `content/portfolio.json` (и можно убрать поле `placeholder` у этой записи, раз она больше не заглушка).
- 2026-09-12: собрана секция «Контакты» на главной — `components/sections/contacts.tsx`, контент в `content/contacts.json`. Указаны: телефон +7 921 554-15-57, ВКонтакте vk.ru/art.ohrey, телеграм-канал @art.ohrey. Для миниатюры ВК по просьбе пользователя скачан реальный аватар сообщества с vk.ru/art.ohrey → `public/images/vk-community-avatar.jpg` (фото Ирины с логотипом ART.OHREY, ~16 КБ, публичный аватар паблика). Иконки соцсетей (VK, Telegram) — простые SVG-заглушки собственного изготовления (`components/icons/vk-icon.tsx`, `telegram-icon.tsx`), не точное воспроизведение официальных логотипов. Между делом на странице сообщества ВК увидели, что часть реальных работ Ирины — фан-арт по чужому IP (DC Comics и т.п.); для сайта это не используем, только собственные придуманные персонажи (см. правило про портфолио выше).
- 2026-09-12: наполнены отдельные страницы `/about`, `/portfolio`, `/contacts` (раньше были однострочные заглушки). `/portfolio` — сетка всех 5 работ (`components/portfolio/portfolio-grid.tsx`, переиспользует `content/portfolio.json` и `portfolio-card.tsx`). `/contacts` — вступительная фраза + карточки контактов; карточки вынесены в переиспользуемый `components/contacts/contact-cards.tsx` (его же теперь использует и секция на главной `components/sections/contacts.tsx`, без дублирования вёрстки). `/about` — портрет-заглушка (тот же градиентный приём, что и в hero, но портретной пропорции) + текст о специализации (только факты, уже зафиксированные в этом файле, без выдуманной биографии) + блок «Как рождается витраж» (3 общих шага изготовления: эскиз → стекло → сборка, без привязки к личным деталям Ирины). Настоящий текст «Обо мне» ещё ждём от Ирины — текстовые черновики (обо мне.txt и т.д.) по её просьбе пока не разбирали.
- 2026-09-12: сделана SEO-база (по правилу «делаем сразу, не откладываем»): `app/sitemap.ts`, `app/robots.ts`, `lib/site-config.ts` (домен, имя, описание сайта в одном месте), canonical URL через `metadata.alternates.canonical` на всех страницах, JSON-LD Organization+Person на главной (`app/page.tsx`, с sameAs на ВК/Телеграм из `content/contacts.json`) и JSON-LD BreadcrumbList на страницах about/portfolio/contacts (`components/seo/breadcrumb-json-ld.tsx`). Важно: домен пока не куплен, в `lib/site-config.ts` временно стоит заглушка `https://art-ohrey.ru` — заменить на реальный, когда домен появится (см. раздел «Доступы»).
- 2026-09-12: код перенесён в GitHub-репозиторий https://github.com/morkelebthennei-hub/site_pavlova (был пустой, с одним авто-коммитом README). На машине не было ни Node.js, ни Git, ни GitHub CLI — установили все три (winget), авторизовались в `gh` через браузерный device-код. Настроена git-идентичность (user.name/user.email). В репозиторий вместе с кодом сайта добавлена копия этого файла как `PROJECT_LOG.md` — чтобы история решений была доступна и вне сессий Claude Code. Личные файлы из `F:\ДаркОхрей` (фото, старые заготовки, папки не по теме сайта) в репозиторий не идут — только содержимое `site-project/`.
- 2026-09-12: настроен GitHub Actions для автодеплоя (`.github/workflows/deploy.yml`) + вся Docker/nginx-инфраструктура из плана: `deploy/Dockerfile` (multi-stage, standalone-сборка Next.js), `deploy/docker-compose.yml`, `deploy/nginx.conf` (reverse proxy + gzip, домен-заглушка art-ohrey.ru), `deploy/vps-setup.sh` (разовая настройка чистого VPS: Docker, nginx, certbot, клонирование репо). В `next.config.ts` добавлен `output: "standalone"`.
  Как это работает: при пуше в `main` джоба `build` всегда гоняет `lint`+`build` (это уже проверяет код). Джоба `deploy` СЕЙЧАС ПРОПУСКАЕТСЯ — в секретах репозитория ещё нет `SSH_HOST`/`SSH_USER`/`SSH_PRIVATE_KEY`, потому что VPS не куплен. Как только сервер появится: прогнать `deploy/vps-setup.sh` на нём (см. инструкции в конце скрипта — DNS, certbot, docker compose up), затем добавить эти три секрета в GitHub (Settings → Secrets and variables → Actions) — деплой включится сам, без правок кода.
  Docker образ и docker-compose локально не тестировались — на машине нет Docker (ставить Docker Desktop не стали, это тяжёлая установка с WSL2/перезагрузкой, не нужная для написания конфигов). Next.js-сборка с `output: standalone` проверена и работает (`npm run build`, `.next/standalone/server.js` создаётся).
  Попутно поймали и починили баг: `npm run lint` до этого никогда не гоняли — обнаружились 2 ошибки правила `react-hooks/set-state-in-effect` (новое строгое правило про setState в effect). Одна — в нашем `components/theme-toggle.tsx` (классический паттерн «дождаться маунта на клиенте» из документации next-themes), переписали через `useSyncExternalStore` вместо `useEffect+useState` — так лint доволен и поведение то же. Вторая — в сгенерированном shadcn-файле `components/ui/carousel.tsx`, который руками не правим (перезапишется при `npx shadcn add`); вместо этого добавили `components/ui/**` в игноры ESLint — это vendor-код shadcn, его не линтуем.
- 2026-09-12: закреплено правило работы (п.7 выше): всё по движку/стеку, бэкенду, дизайну, доступам и деплою — фиксировать в этом файле сразу и самостоятельно, без напоминаний, и синхронизировать с `PROJECT_LOG.md` в репозитории при каждом пуше.
- 2026-09-12: пользователь купил хостинг на reg.ru и дал доступ для деплоя. При подключении выяснилось, что это виртуальный хостинг с ISPmanager, а не VPS с root (подробности — в разделе «Инфраструктура и деплой» выше). Установили личный SSH-ключ на сервер (пароль использовали только один раз для этого, нигде не хранится). Docker-план не подходит; предложили статическую сборку как альтернативу. Пользователь решил сам уточнить у поддержки reg.ru тип тарифа/возможность Node.js или root, прежде чем продолжать — деплой отложен, ничего на сайт не заливали.
- 2026-09-12: пользователь получил второй сервер от reg.ru — на этот раз настоящий VPS с root, Ubuntu 26.04 LTS, подтверждено по SSH. Установили тот же SSH-ключ. Обнаружили, что сервер маленький (1 CPU / ~1 ГБ RAM без swap) — сборка Docker-образа прямо на нём рискованна. Поменяли архитектуру деплоя: образ теперь собирается в GitHub Actions и публикуется в GitHub Container Registry, а сервер только скачивает готовый образ. Изменения: `.github/workflows/deploy.yml` (новая джоба `image`), `deploy/docker-compose.yml` (`image:` вместо `build:`), `deploy/vps-setup.sh` (добавлено создание swap), `deploy/nginx.conf` и `lib/site-config.ts` (домен-заглушка заменена на настоящий `art-ohrey.art`). Первый (shared-хостинг) сервер для проекта больше не используется.
- 2026-09-12: первый деплой на сервер выполнен успешно. Прогнали `deploy/vps-setup.sh` (swap, Docker, nginx, certbot, клонирование репозитория). Добавили секреты в GitHub, GHCR-пакет оказался публичным сам по себе. Автодеплой проверен — все три джобы (build/image/deploy) проходят зелёным, сайт отвечает 200 OK на сервере и через nginx. Осталось: пользователь переключает DNS домена на новый сервер, затем выпускаем HTTPS через certbot.
